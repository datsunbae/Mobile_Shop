@model IEnumerable<Phone_Ecommerce_Manage.Models.Rom>

@{
    ViewData["Title"] = "Danh sách dung lượng bộ nhớ";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="main-container">
    <div>
        <div class="min-height-200px">
            <div class="card-box mb-30">
                <div class="pd-20">
                    <h4 class="text-blue h4">Danh sách dung lượng bộ nhớ</h4>
                    <a asp-area="Admin" asp-controller="Roms" asp-action="Create" type="button" class="btn btn-primary">
                        Thêm dung lượng bộ nhớ
                    </a>
                </div>
                <div class="pb-20">
                    <table class="data-table table stripe hover nowrap">
                        <thead>
                            <tr>
                                <th>Dung lượng bộ nhớ</th>
                                <th class="table-plus datatable-nosort"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.NameRom</td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.IdRom" class="btn btn-primary">Sửa</a>
                                        <a asp-action="Delete" asp-route-id="@item.IdRom" class="btn btn-danger">Xóa</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
